#!/usr/bin/env bash

# Please call this file in package json or "run npm run sync-packages"

for item in 'app' 'auth'; do
    ln -sfn ../package.json resources/assets/$item/
    ln -sfn ../yarn.lock resources/assets/$item/
done

exit
